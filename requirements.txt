# Python packages the module needs inside Odoo's Python environment.
# Windows installer:  "C:\Program Files\Odoo <version>\python\python.exe" -m pip install -r requirements.txt
# Linux / Docker:     pip install -r requirements.txt   (same environment that runs odoo-bin)
pypdf>=3.0
python-docx>=1.0
requests>=2.25
